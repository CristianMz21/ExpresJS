# ===========================================
# APPLICATION CONFIGURATION
# ===========================================
# Puerto en el que correrá la aplicación Express
PORT=3000

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# ===========================================
# POSTGRESQL DATABASE CONFIGURATION
# ===========================================
# Usuario de PostgreSQL
POSTGRES_USER=postgres

# Contraseña de PostgreSQL (CAMBIAR EN PRODUCCIÓN)
POSTGRES_PASSWORD=POSTGRES

# Nombre de la base de datos
POSTGRES_DB=miapp_db

# Directorio de datos de PostgreSQL
PGDATA=/var/lib/postgresql/data/pgdata

# Puerto de PostgreSQL
POSTGRES_PORT=5432

# Nombre del contenedor de PostgreSQL
POSTGRES_CONTAINER_NAME=postgres_db

# Nombre del volumen de PostgreSQL
POSTGRES_VOLUME_NAME=postgres_data

# ===========================================
# PGADMIN CONFIGURATION
# ===========================================
# Email para acceder a pgAdmin
PGADMIN_EMAIL=admin@admin.com

# Contraseña para acceder a pgAdmin (CAMBIAR EN PRODUCCIÓN)
PGADMIN_PASSWORD=admin

# Puerto de pgAdmin
PGADMIN_PORT=5050

# Nombre del contenedor de pgAdmin
PGADMIN_CONTAINER_NAME=pgadmin

# Nombre del volumen de pgAdmin
PGADMIN_VOLUME_NAME=pgadmin_data

# ===========================================
# EXPRESS APP CONFIGURATION (DOCKER)
# ===========================================
# Nombre del contenedor de la aplicación Express
APP_CONTAINER_NAME=express_app

# ===========================================
# NETWORK CONFIGURATION
# ===========================================
# Nombre de la red Docker
NETWORK_NAME=express_app_network

# ===========================================
# DATABASE URL (para usar con ORMs como Sequelize, TypeORM, Prisma)
# ===========================================
# URL de conexión completa a la base de datos
# Formato: postgres://usuario:contraseña@host:puerto/nombre_db
DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# Para conectar desde dentro de Docker, usar 'db' como host:
# DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}

# ===========================================
# SECURITY & SECRETS (AGREGAR SEGÚN NECESITES)
# ===========================================
# JWT Secret para autenticación
# JWT_SECRET=tu_secreto_super_seguro_aqui

# Clave de encriptación
# ENCRYPTION_KEY=tu_clave_de_encriptacion

# CORS Origins permitidos (separados por coma)
# CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ===========================================
# LOGGING & MONITORING
# ===========================================
# Nivel de logging (error, warn, info, debug)
# LOG_LEVEL=info

# ===========================================
# RATE LIMITING
# ===========================================
# Ventana de tiempo para rate limiting (en minutos)
# RATE_LIMIT_WINDOW_MS=15

# Máximo de requests por ventana
# RATE_LIMIT_MAX_REQUESTS=100
